name: Nexus Repository Maintenance

on:
  workflow_dispatch:
    inputs:
      run_weekly_maintenance:
        description: Run safe Git object repacking and The Crucible
        required: false
        type: boolean
        default: false
  schedule:
    - cron: '17 3 * * *'
    - cron: '47 4 * * 0'

permissions:
  contents: read

jobs:
  daily-clutter-audit:
    name: Daily clutter audit · ${{ matrix.ref }}
    strategy:
      fail-fast: false
      matrix:
        ref: [main, Development-branch]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ matrix.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
      - run: npm run repository:clutter
      - run: npm run inventory:verify
      - run: npm run privacy:verify

  weekly-git-maintenance:
    name: Weekly safe Git repack · ${{ matrix.ref }}
    if: github.event.schedule == '47 4 * * 0' || inputs.run_weekly_maintenance == true
    strategy:
      fail-fast: false
      matrix:
        ref: [main, Development-branch]
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ matrix.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
      - name: Skip repository code execution in privileged maintenance workflow
        run: echo "Repository code execution is intentionally disabled here to prevent cache-poisoning paths."
