node_modules/
dist/
*.log
.DS_Store
push/

# Local Nexus identities, account state, credentials, and signing material.
.env
.env.*
*.pfx
*.p12
*.key
account-vault*
nexus-user-data*
nexus-config.json

# Never commit local Git repository internals or accidental duplicate exports.
.git/
COMMIT_EDITMSG
FETCH_HEAD
ORIG_HEAD
packed-refs
*[[]1[]]*

# The Crucible CI run artifacts: regenerated every run, never committed.
/.the-crucible-report.json
/.the-crucible-quarantine/
/.the-crucible-runtime/
